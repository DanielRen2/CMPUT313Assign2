CMPUT313 Assignment 2 Routing Algorithm Comparison
By Xiao Yu Ren and Beikun Yan

Instructions:
To use this simulator you need to run the the program using python3. The input file name also needs to be specified.

python3 hsim.py filename.txt

**This simulator has several limitations
**It will crash if the node has no neighbours
**it will crash if only one node exists

The data is outputed into the command window and into a Output.csv file
